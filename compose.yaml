services:
  pool-arena:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: 10000
      APP_ENV: development
    ports:
      - "8080:10000"
    volumes:
      - pool-data:/var/lib/pool

volumes:
  pool-data:
