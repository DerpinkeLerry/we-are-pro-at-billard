# Nothing is required for the single-service Render deployment.
# These values are optional overrides for local/development use.
APP_ENV=development
PORT=10000
SQLITE_PATH=/var/lib/pool/pool.sqlite
# JOIN_TOKEN_SECRET=optional-32-plus-byte-secret
# GAME_INTERNAL_SECRET=optional-32-plus-byte-secret
